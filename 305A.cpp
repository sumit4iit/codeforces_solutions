#include<iostream>
using namespace std;

int main()
{
	int n;
	cin>>n;
	int a[4] = {0};
	int temp;
	int count = 0;
	vector<int> ans;
 	for(int =0; i< n ; i++)
	{
		cin>>temp;
		if(temp == 0)
		{
			vector.push_back(temp);
			count++;
		}
		else if(temp > 0 && temp <10 && !a[1])
		{
			count++;
			vector.push_back(temp);
			a[1] = 1;
		}
		else if(temp > 9 && temp)
		
		
	}
}
